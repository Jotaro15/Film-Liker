import 'package:flutter/material.dart';

class FilmsLikes extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Mes films likés'),
      ),
      body: Center(
        child: Text('Contenu de la page FilmsLikes'),
      ),
    );
  }
}
